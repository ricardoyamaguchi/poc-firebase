# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'poc-firebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'

  # Pods for poc-firebase

  target 'poc-firebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'poc-firebaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end


end
